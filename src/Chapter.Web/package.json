{
  "name": "chapter-web",
  "version": "1.0.0",
  "private": true,
  "description": "Front-end for Chapter, served from a WebView2 virtual host",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dompurify": "^3.4.13",
    "marked": "^18.0.9",
    "monaco-editor": "^0.56.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^5.9.3"
  }
}
